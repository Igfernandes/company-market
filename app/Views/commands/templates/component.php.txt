<?php
/**
 * Template de geração de componentes
 *
 * ⚠️ Este arquivo contém placeholders (ex: {{className}}, {{namespace}}) que
 * são substituídos dinamicamente pelo comando `php spark component:make`.
 *
 */

namespace {{namespace}};

use App\Components\BaseComponents;

class {{className}} extends BaseComponents
{
    const ORIGIN = "{{origin}}";
    const PROPS = [];

    public static function render()
    {
        Component(self::ORIGIN, compact(self::PROPS));
    }
}
